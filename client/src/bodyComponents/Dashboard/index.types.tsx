export interface progressProps{
    bgcolor: string;
    progress: string;
    height: string;
}

export interface tableDataProps{
    task: string,
    points: string,
    finished: boolean
}